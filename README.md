## Benchmark files and example usage for h3_turbo

Wheel files containing the Python bindings and benchmarnk notebooks are in the `dist` directory.
Docker images for Nvidia CUDA compute capability 8.9, 9.0, 10.0  and 12.0 with Jupyter lab are available at `https://hub.docker.io/cflockhart/h3_turbo_sm_89:latest`, `https://hub.docker.io/cflockhart/h3_turbo_sm_90:latest` etc.